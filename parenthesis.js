//find out the odd parenthesis that stays in your string which you take as an input
//suppose [()]({(])} is input
//then 2 is your output

let brackets = "[()]({(])}"
let len = brackets.length;
let brac = [];
let a = brackets.indexOf('[');
console.log(len)
console.log(a)